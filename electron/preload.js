// All Node.js APIs are available in the preload process.
// You can expose safe APIs to the renderer via contextBridge here if needed.
window.addEventListener('DOMContentLoaded', () => {
  // Preload logic (e.g., expose APIs)
});
